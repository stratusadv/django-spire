from django.urls.conf import include, path


app_name = '${spire_app_name}'

urlpatterns = [
    path('page/', include('${module}.urls.page_urls', namespace='page')),
    path('form/', include('${module}.urls.form_urls', namespace='form')),
]
